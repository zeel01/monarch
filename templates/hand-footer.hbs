{{ log "outer" fields.name }}
{{#> "modules/monarch/templates/parts/footer.hbs"}}
	<!--{{!	
		This must be conditional because the helper gets called out of context for some reason when
		passing to `@partial-block`. The HTML comment prevents it from being treated as empty.
	}}-->
	{{#if fields.name}}
		{{formInput fields.name value=source.name placeholder=fields.name.label aria=(object label=fields.name.label)}}
	{{/if}}
{{/ "modules/monarch/templates/parts/footer.hbs"}}